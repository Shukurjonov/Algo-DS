// const fs = require('fs');
// let data = fs.readFileSync('input.txt', 'utf-8');

// data = data.split('\n').map( el => BigInt(el));
// let n = data[0];
//  for (let i = 1; i <= n; i++) {
//     let son = (data[i] * data[i]) % BigInt(10 ** 9 + 7);
//     process.stdout.write( '' + son  + '\n');
// }

